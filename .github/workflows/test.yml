# Define the name of the workflow
name: test.yml

# Use the `push-trigger.yml` template
extends: .github/workflows/push-trigger.yml@v1.0

# Define the event triggers for the workflow
on:
  push:
    branches:
      - '*'
  pull_request:
    branches:
      - '*'
  workflow_dispatch:    

jobs:
  build:
    uses: .github/workflows/build.yml@v1.0

  deploy:
    uses: .github/workflows/deploy.yml@v1.0
